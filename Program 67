#include <stdio.h>

// Define a union
union MyUnion {
    int i;
    float f;
    char c;
};

int main() {
    // Declare a union variable
    union MyUnion myUnion;

    // Find the size of the union using sizeof
    printf("Size of union: %zu bytes\n", sizeof(myUnion));

    return 0;
}
